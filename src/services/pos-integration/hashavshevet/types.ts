/**
 * @fileoverview Defines types specific to the Hashavshevet POS system.
 * (This is a placeholder for future implementation).
 */

/**
 * Defines the specific credentials required to connect to the Hashavshevet API.
 */
export interface HashavshevetCredentials {
  apiKey: string;
  companyFile: string;
}
